module.exports = {
    // Request
    0x00: {
        name: "Request",
        fields: {
            // Doesn't have any fields
        }
        
    },

    // Ping
    0x01: {
        name: "Ping",
        fields: {
            payload: "Long"
        }
    }
}